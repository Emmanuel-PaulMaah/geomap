// Flags are now displayed as images from REST Countries API
// This utility can be deprecated or used for other flag-related functionality

export const getCountryFlag = (cca2) => {
  // This function is now deprecated - use country.flags.png from REST Countries API instead
  // Kept for backwards compatibility
  return null
}
